<?php
/**
 * Doctype declaration
 *
 * Declares the html doctype used on the page.
 *
 * @package Slimline / Theme
 * @see     https://developer.mozilla.org/en-US/docs/Glossary/Doctype
 *          Description of the `doctype` declaration
 * @since   0.2.0
 */

if ( ! defined( 'ABSPATH' ) ) exit; // exit if accessed directly

/**
 * Make sure not to have any spaces or newlines before the declaration
 */
?><!doctype html>